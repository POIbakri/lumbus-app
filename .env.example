# Supabase Configuration
# Get these from: https://app.supabase.com/project/_/settings/api
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Stripe Configuration
# Get from: https://dashboard.stripe.com/apikeys
# Use TEST keys for development: pk_test_...
# Use LIVE keys for production: pk_live_...
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key-here

# API Configuration
EXPO_PUBLIC_API_URL=https://your-api-domain.com/api

# EAS Project ID (optional, only needed for EAS builds)
# EAS_PROJECT_ID=your-eas-project-id-here

# Google Services JSON path (optional, only needed for Android builds with Firebase)
# GOOGLE_SERVICES_JSON=./path/to/google-services.json

# Setup Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit .env to git (it's in .gitignore)
# 4. For production builds, use EAS Secrets instead of .env
